/**
 * @author Kaden Badalian
 *
 * @filename actionTypes.js
 * @date 4/7/20
 */

import { createReducer } from '@reduxjs/toolkit';
import {
} from '../actionTypes';

const defaultState = {
};

const browseReducer = createReducer(defaultState, {
});

export default browseReducer;
